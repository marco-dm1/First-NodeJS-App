function myThirdFunction(){
    console.log("My third module's function is called");
}

module.exports = myThirdFunction // We can also export without defining the function because there is only one function